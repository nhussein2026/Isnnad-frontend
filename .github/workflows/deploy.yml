name: Deploy to Vercel

on:
  push:
    branches:
      - main   # or whatever branch you want

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: |
          curl -X POST "$ISNNAD_VERCEL_DEPLOY_HOOK_URL"
        env:
          ISNNAD_VERCEL_DEPLOY_HOOK_URL: ${{ secrets.ISNNAD_VERCEL_DEPLOY_HOOK_URL }}
